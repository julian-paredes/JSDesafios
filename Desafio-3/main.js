// Desafio 3 JS - Bucles

let numero = Number (prompt("Ingrese un número:"));

for (let i=1; i<=10; i++){

    let resultado = numero*i; // Tablas de multiplicar
    console.log(numero + "x" + i + "=" + resultado);
    alert(numero + "x" + i + "=" + resultado);
}